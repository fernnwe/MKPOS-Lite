<UserControl x:Class="MKPOS.App.Views.Modules.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MKPOS.App.Converters">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barra de herramientas -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="12,10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Nueva categoría" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NewCommand}"/>
                    <Button Content="Editar" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Command="{Binding EditCommand}"/>
                    <Button Content="Desactivar" Style="{StaticResource DangerButtonStyle}" Margin="8,0,0,0" Command="{Binding DeleteCommand}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox Content="Mostrar inactivas" VerticalAlignment="Center"
                              IsChecked="{Binding IncludeInactive}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Lista de categorías -->
        <DataGrid Grid.Row="1" Style="{StaticResource ModuleTableStyle}"
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedRow}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Productos" Binding="{Binding ProductCount}" Width="110"/>
                <DataGridTextColumn Header="Estado" Width="100">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsActive">
                            <Binding.Converter>
                                <local:BoolToTextConverter TrueText="Activa" FalseText="Inactiva"/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Editor de categoría -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="16,12" Margin="0,10,0,0"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
            <StackPanel>
                <TextBlock Text="{Binding EditorTitle}" FontSize="15" FontWeight="SemiBold" Foreground="#1F3864" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" Width="280">
                        <TextBlock Text="Nombre" Foreground="#374151"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="8,5" BorderBrush="#CBD5E1" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Descripción" Foreground="#374151"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Padding="8,5" BorderBrush="#CBD5E1" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10" VerticalAlignment="Bottom">
                        <CheckBox Content="Categoría activa" IsChecked="{Binding EditorIsActive}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button Content="Cancelar" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelEditCommand}"/>
                        <Button Content="Guardar" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0" Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" TextWrapping="Wrap" Margin="0,10,0,0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NonEmptyToVisibility}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>