<UserControl x:Class="MKPOS.App.Views.Modules.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MKPOS.App.Converters">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="12,10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Actualizar" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding RefreshCommand}"/>
                <TextBlock Text="Últimas 100 ventas" Foreground="#6B7280" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" Style="{StaticResource ModuleTableStyle}"
                  ItemsSource="{Binding Sales}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Folio" Binding="{Binding TicketNumber}" Width="70"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding SaleDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="130"/>
                <DataGridTextColumn Header="Artículos" Binding="{Binding ItemCount}" Width="80"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat={}{0:N2}}" Width="90"/>
                <DataGridTextColumn Header="IVA" Binding="{Binding TaxAmount, StringFormat={}{0:N2}}" Width="90"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="Método" Binding="{Binding PaymentMethodName}" Width="100"/>
                <DataGridTextColumn Header="Cliente" Binding="{Binding CustomerName}" Width="140"/>
                <DataGridTextColumn Header="Estado" Width="110">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsCancelled">
                            <Binding.Converter>
                                <local:BoolToTextConverter TrueText="Cancelada" FalseText="Completada"/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>