<Window x:Class="MKPOS.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MKPOS Lite - Iniciar sesión"
        Height="440" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI" FontSize="13"
        Background="#F5F6FA">
    <Window.InputBindings>
        <KeyBinding Key="Return" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" />
    </Window.InputBindings>

    <Grid Margin="36,28,36,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MKPOS Lite"
                   HorizontalAlignment="Center"
                   FontSize="30" FontWeight="Bold"
                   Foreground="#1F3864"/>

        <TextBlock Grid.Row="1" Text="Punto de venta"
                   HorizontalAlignment="Center"
                   Margin="0,2,0,24"
                   FontSize="13" Foreground="#6B7280"/>

        <TextBlock Grid.Row="2" Text="Usuario" Foreground="#374151"/>
        <TextBox Grid.Row="3" x:Name="UsernameBox"
                 Margin="0,4,0,14" Padding="8,6"
                 BorderBrush="#CBD5E1" BorderThickness="1"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Text="Contraseña" Foreground="#374151"/>
        <PasswordBox Grid.Row="5" x:Name="PasswordBox"
                     Margin="0,4,0,14" Padding="8,6"
                     BorderBrush="#CBD5E1" BorderThickness="1"/>

        <TextBlock Grid.Row="6" Text="{Binding ErrorMessage}"
                   Foreground="#DC2626" TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NonEmptyToVisibility}}"/>

        <Button Grid.Row="8" Content="Entrar"
                Height="40"
                FontSize="14" FontWeight="SemiBold"
                Foreground="White" Background="#1F3864"
                BorderThickness="0" Cursor="Hand"
                IsDefault="True"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#1F3864"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#16294B"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#9CA3AF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>